# Use postgres/example user/password credentials
version: '3.1'

services:
    postgres:
        image: postgres:9
        hostname: postgres
        container_name: postgres
        ports:
          - 5432:5432
